任务：在线考试管理系统-------用户信息管理模块

子任务：
    用户信息注册
    用户信息查询
    用户信息删除
    用户信息更新

准备工作：
    1. 创建用户信息表 Users.frm
        create table Users(
        userID int primary key auto_increment,  #用户编号
        userName varchar(50),    #用户名称
        password varchar(50),    #用户密码
        sex      char(1),        #用户性别: ‘男’/‘女’
        email    varchar(50)     #用户电邮
        )

    2. 在src目录下准备users实体类

    3. 在src目录下导入util包